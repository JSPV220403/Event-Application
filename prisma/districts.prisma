model Districts{
  id String @id @default(uuid())
  name String @db.VarChar(50)
  state_id String @default(uuid())
  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  state States @relation(fields: [state_id], references: [id])
  address Addresses[]
}